# Archived Backend Plan

This file is historical and non-authoritative.

It records the original implementation plan for `talk`, `tell`, `audio`, and narration support. Do not use it as runtime guidance. Current behavior is defined by the skill files under `podcast/skills/` and by the Python scripts under `podcast/skills/audio/scripts/`.
